/**
 * @type {import('@fluxpress/core').FluxPressConfig}
 */
export default {
  github: {
    owner: '<%= config.owner %>',
    repo: '<%= config.repo %>',
    need_data: [],
  },
  theme: '<%= config.theme %>',
}
